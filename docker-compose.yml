version: '3.8'

services:
  beam-ui:
    build: .
    ports:
      - "3000:5173"  # Map host port 3000 to container port 5173 (Nginx)
    environment:
      - NODE_ENV=production
    restart: unless-stopped