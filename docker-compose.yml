version: '3.8'

services:
  beam-ui:
    build: .
    ports:
      - '3000:80' # Map host port 3000 to container port 80 (Nginx)
    environment:
      - NODE_ENV=production
    restart: unless-stopped
